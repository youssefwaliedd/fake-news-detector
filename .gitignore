# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# --- Secrets ---
.env
backend/.env
frontend/.env

# --- ML training data (large; regenerate via download) ---
backend/data/
# NOTE: the trained model artifacts (app/ml/artifacts/*.pkl, ~1 MB) ARE committed
# so the deployed app ships with the classifier. Retrain with `python -m app.ml.train`.

# --- Node / Vite ---
node_modules/
frontend/dist/

# --- OS / editors ---
.DS_Store
*.log
.idea/
.vscode/
